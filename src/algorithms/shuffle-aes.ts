export const encrypt = (data: Uint8Array, key: string): Uint8Array => {
	return new Uint8Array();
};

export const decrypt = (data: Uint8Array, key: string): Uint8Array => {
	return new Uint8Array();
};
